import { Badge } from "@/components/ui/badge";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { titleCase } from "@/lib/format";
import { cn } from "@/lib/utils";
import type { SystemUser } from "@/types/management";

export function UsersTable({ users }: { users: SystemUser[] }) { return <div><div className="hidden overflow-x-auto sm:block"><Table><TableHeader><TableRow><TableHead>User</TableHead><TableHead>Role</TableHead><TableHead>Status</TableHead><TableHead>Last active</TableHead></TableRow></TableHeader><TableBody>{users.map((user) => <TableRow key={user.id}><TableCell><p className="font-medium">{user.name}</p><p className="text-xs text-muted-foreground">{user.email}</p></TableCell><TableCell>{user.role}</TableCell><TableCell><Badge variant="outline" className={cn(user.status === "active" ? "border-status-success/25 bg-status-success/8 text-status-success" : user.status === "invited" ? "border-status-warning/25 bg-status-warning/8 text-status-warning" : "bg-muted text-muted-foreground")}>{titleCase(user.status)}</Badge></TableCell><TableCell className="text-xs text-muted-foreground">{user.lastActive}</TableCell></TableRow>)}</TableBody></Table></div><div className="divide-y sm:hidden">{users.map((user) => <article key={user.id} className="space-y-3 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{user.name}</p><p className="text-xs text-muted-foreground">{user.email}</p></div><Badge variant="outline">{titleCase(user.status)}</Badge></div><p className="text-xs">{user.role} · {user.lastActive}</p></article>)}</div></div>; }
