import { formatCurrency } from "@/lib/format";

const revenue = [{ day: "Mon", amount: 3220 }, { day: "Tue", amount: 3890 }, { day: "Wed", amount: 3410 }, { day: "Thu", amount: 4260 }, { day: "Fri", amount: 4620 }, { day: "Sat", amount: 5180 }, { day: "Sun", amount: 4820 }];
const max = Math.max(...revenue.map((item) => item.amount));

export function RevenueOverview() { return <div><div className="flex items-end justify-between"><div><p className="text-xs text-muted-foreground">Last 7 days</p><p className="mt-1 text-xl font-semibold">{formatCurrency(revenue.reduce((sum, item) => sum + item.amount, 0))}</p></div><p className="text-xs font-medium text-status-success">+12.6% this week</p></div><div className="mt-6 grid h-44 grid-cols-7 items-end gap-2" aria-label="Revenue for the last seven days">{revenue.map((item) => <div key={item.day} className="flex h-full flex-col justify-end gap-2"><div className="group relative flex flex-1 items-end"><span className="absolute left-1/2 z-10 hidden -translate-x-1/2 rounded bg-foreground px-2 py-1 text-[10px] text-background group-hover:block">{formatCurrency(item.amount)}</span><span className="w-full rounded-t bg-primary/80 transition-colors hover:bg-primary" style={{ height: `${Math.max(14, (item.amount / max) * 100)}%` }} /></div><span className="text-center text-[10px] text-muted-foreground">{item.day}</span></div>)}</div></div>; }
