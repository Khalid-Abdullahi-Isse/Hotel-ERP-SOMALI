import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { TransactionStatusBadge } from "@/components/finance/finance-status";
import { formatCurrency, formatShortDate } from "@/lib/format";
import type { ExpenseRecord } from "@/types/finance";

export function ExpensesTable({ expenses }: { expenses: ExpenseRecord[] }) { return <div><div className="hidden overflow-x-auto md:block"><Table><TableHeader><TableRow><TableHead>Reference</TableHead><TableHead>Date</TableHead><TableHead>Category</TableHead><TableHead>Recorded by / description</TableHead><TableHead>Status</TableHead><TableHead className="text-right">Amount</TableHead></TableRow></TableHeader><TableBody>{expenses.map((expense) => <TableRow key={expense.id}><TableCell className="font-mono text-xs text-primary">{expense.reference}</TableCell><TableCell>{formatShortDate(expense.date)}</TableCell><TableCell className="font-medium">{expense.category}</TableCell><TableCell><p className="font-medium">{expense.vendor}</p><p className="mt-0.5 text-[11px] text-muted-foreground">{expense.description}</p></TableCell><TableCell><TransactionStatusBadge status={expense.status} /></TableCell><TableCell className="text-right font-semibold tabular-nums">{formatCurrency(expense.amount, expense.currency)}</TableCell></TableRow>)}</TableBody></Table></div><div className="divide-y md:hidden">{expenses.map((expense) => <article key={expense.id} className="space-y-3 p-4"><div className="flex justify-between gap-3"><div><p className="font-medium">{expense.vendor}</p><p className="text-xs text-muted-foreground">{expense.category} · {expense.reference}</p></div><p className="font-semibold">{formatCurrency(expense.amount, expense.currency)}</p></div><p className="text-xs leading-5 text-muted-foreground">{expense.description}</p><TransactionStatusBadge status={expense.status} /></article>)}</div><div className="border-t px-4 py-3 text-xs text-muted-foreground">{expenses.length} expense records</div></div>; }
