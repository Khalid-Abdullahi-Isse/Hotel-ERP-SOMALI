"use client";

import { useState } from "react";
import { Search } from "lucide-react";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { titleCase } from "@/lib/format";
import { cn } from "@/lib/utils";
import type { EmployeeDepartment, EmployeeRecord } from "@/types/management";

const departments: EmployeeDepartment[] = ["Front Desk", "Housekeeping", "Management", "Accounting", "Maintenance"];
function initials(name: string) { return name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase(); }
export function EmployeesTable({ employees }: { employees: EmployeeRecord[] }) { const [search, setSearch] = useState(""); const [department, setDepartment] = useState("all"); const query = search.trim().toLowerCase(); const visible = employees.filter((employee) => (!query || [employee.name, employee.employeeCode, employee.role, employee.phone].some((value) => value.toLowerCase().includes(query))) && (department === "all" || employee.department === department)); return <div><div className="flex flex-col gap-3 border-b p-4 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input value={search} onChange={(event) => setSearch(event.target.value)} type="search" placeholder="Search employee, role, or phone" aria-label="Search employees" className="h-9 pl-9" /></div><Select value={department} onValueChange={setDepartment}><SelectTrigger className="h-9 w-full sm:w-48" aria-label="Filter department"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All departments</SelectItem>{departments.map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="hidden overflow-x-auto md:block"><Table><TableHeader><TableRow><TableHead>Employee</TableHead><TableHead>ID</TableHead><TableHead>Department</TableHead><TableHead>Phone</TableHead><TableHead>Shift</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{visible.map((employee) => <TableRow key={employee.id}><TableCell><div className="flex items-center gap-3"><Avatar className="size-8"><AvatarFallback className="bg-secondary/10 text-xs font-semibold text-secondary">{initials(employee.name)}</AvatarFallback></Avatar><div><p className="font-medium">{employee.name}</p><p className="text-[11px] text-muted-foreground">{employee.role}</p></div></div></TableCell><TableCell className="font-mono text-xs text-primary">{employee.employeeCode}</TableCell><TableCell>{employee.department}</TableCell><TableCell>{employee.phone}</TableCell><TableCell>{employee.shift}</TableCell><TableCell><Badge variant="outline" className={cn(employee.status === "on_duty" ? "border-status-success/25 bg-status-success/8 text-status-success" : employee.status === "on_leave" ? "border-status-warning/25 bg-status-warning/8 text-status-warning" : "bg-muted text-muted-foreground")}>{titleCase(employee.status)}</Badge></TableCell></TableRow>)}</TableBody></Table></div><div className="divide-y md:hidden">{visible.map((employee) => <article key={employee.id} className="flex items-start gap-3 p-4"><Avatar><AvatarFallback className="bg-secondary/10 text-xs font-semibold text-secondary">{initials(employee.name)}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><p className="font-medium">{employee.name}</p><p className="text-xs text-muted-foreground">{employee.role}</p></div><Badge variant="outline">{titleCase(employee.status)}</Badge></div><p className="mt-3 text-xs">{employee.department} · {employee.shift} shift</p><p className="mt-1 text-xs text-muted-foreground">{employee.phone}</p></div></article>)}</div><div className="border-t px-4 py-3 text-xs text-muted-foreground">{visible.length} of {employees.length} employees</div></div>; }
